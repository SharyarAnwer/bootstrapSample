import React, { useState } from "react";

export default function HookCounter(props) {
  /* const [count, setCount] = useState(props.startFrom);
  
    let counter = props.startFrom; */

  return (
    <div>
      {/* <button
        onClick={() => {
          /* setCount(count + 1)

          counter++;
          console.log(counter);
        }}
      >
        {counter}
      </button> */}
    </div>
  );
}
